
spring:  cloud:    config:
      discovery:
        enabled: true        service-id: cloud-config
management:  endpoints:    web:
      exposure:
        include:        - refresh       