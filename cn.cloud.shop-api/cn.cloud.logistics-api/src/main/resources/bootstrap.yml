

    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  
      
spring:
  cloud:
    config:
      label: master
      profile: dev
#      uri:
#      - http://localhost:8768/
      discovery:
        enabled: true
        service-id: cloud-config
    bus:
      enabled: true
      trace:
        enabled: true
        

        
management:
  endpoints:
    web:
      exposure:
        include:
        - bus-refresh


       
