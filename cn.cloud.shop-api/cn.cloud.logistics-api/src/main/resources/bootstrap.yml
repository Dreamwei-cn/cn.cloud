
spring:  cloud:    config:
      enabled: true
      uri:      - http://localhost:8768/      name: service-logistics
      profile: dev
      label: master

      
management:
  endpoints:
    web:
      exposure:
        include:
        - refresh
