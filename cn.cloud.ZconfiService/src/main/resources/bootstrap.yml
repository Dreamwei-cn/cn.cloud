server:
  port: 8768
  
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/     
spring:
  application:
    name: cloud-config
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/CNMWDream/springCloud-config
      label: master
      username: 
      password: 
          